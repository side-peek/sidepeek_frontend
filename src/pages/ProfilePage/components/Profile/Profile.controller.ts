import { NavigateFunction } from "react-router-dom"

export const handleNewProject = (navigate: NavigateFunction) => {
  navigate("/test")
}

export const handleEditProfile = (navigate: NavigateFunction) => {
  navigate("/test")
}
