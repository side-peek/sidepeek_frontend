import { Button } from "@chakra-ui/react"

const LoginButton = () => {
  return (
    <Button
      type="submit"
      p="3rem"
      backgroundColor="blue.100"
      color="white"
      fontSize="2.4rem">
      로그인
    </Button>
  )
}

export default LoginButton
